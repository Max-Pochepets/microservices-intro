apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-db-service" .Values.songs.name }}
  namespace: {{ required "namespace is required!" .Values.namespace }}
spec:
  selector:
    app: {{ printf "%s-db" .Values.songs.name }}
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
